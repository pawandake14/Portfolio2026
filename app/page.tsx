export default function Home() {
  return (
    <div className="flex flex-col items-center justify-center min-h-[80vh] w-full">
      {/* Temporary placeholder to ensure styling works */}
      <h1 className="text-5xl font-bold text-transparent bg-clip-text bg-gradient-to-r from-blue-400 to-purple-500">
        Portfolio Home
      </h1>
      <p className="mt-4 text-gray-400">
        The default Next.js boilerplate is gone. Ready to build the hero
        section!
      </p>
    </div>
  );
}
